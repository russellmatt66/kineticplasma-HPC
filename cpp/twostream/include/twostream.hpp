#include <vector>

#include "../espic_1d1v/include/Particles.hpp"

// Function to concatenate particle species together into ParticleList
size_t ConcatenateParticles(std::vector<ParticleSpecies1d1v> &ParticleList){
    size_t status = 0;
    /* 
    Concatenate the different Particle Species together into ParticleList
    */
    return status;
}

/*
Refactor library functions from "../espic_1d1v/include/espic1d1v.hpp" to work with ParticleList
*/ 